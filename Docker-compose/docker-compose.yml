version: '3.8'

services:
  prometheus:
    image: prom/prometheus
    container_name: prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana
    container_name: grafana
    environment:
      GF_SECURITY_ADMIN_USER: admin_devoteam         
      GF_SECURITY_ADMIN_PASSWORD: htgawm@@   
    ports:
      - "3000:3000"

  akhq:
    build: 
      context: .
      dockerfile: Dockerfile
    container_name: akhq
    volumes:
      - ./application.yml:/app/application.yml
    ports:
      - "8080:8080"
